# Local development .env
VITE_API_BASE_URL=http://34.50.7.78:8080
VITE_VIDEO_BASE_URL=http://34.50.7.78:8000

# Vercel deployment environment variables
# Leave these empty in Vercel so the frontend calls same-origin /api and /video rewrites.
# MJPEG video through Vercel rewrite can be unstable; keep local preview as the video demo fallback.
# VITE_API_BASE_URL=
# VITE_VIDEO_BASE_URL=
